// sample.ul
int factorial (int n) {
	int ret; 
	if (n == 1) {
		ret = 1;
	}
	else {
		ret = n*factorial(n-1);
	}
	return ret; 
}

// And some more comments
// Here they are

void main () {
	print "The factorial of 8 is ";  
	println factorial(8); // Another comment
}
