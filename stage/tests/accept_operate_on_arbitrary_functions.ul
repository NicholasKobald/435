void main() {
    int x;

    foo();

    foo() + bar();

    x = foo() + foo() + foo() + foo() + foo() + foo() + foo() + foo();

    return bar() + bar() + bar() + bar() + bar() + bar() + bar() * 2;
}

int foo() {
    return 1;
}

int bar() {
    return 2;
}

void some_wierd_shit() {
    return multiply_numbers(
        50000000, 300000);

}


int multiply_numbers(int a, int b) {
    return a * b;
}
